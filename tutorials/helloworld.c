#include <stdlib.h>
#include <stdio.h>

int main(int argc, char **argv) {
    printf("Hello, world!\n"); // print to the console
    
    return 0;                  // 0 means success
                               // this will be the return code of the program
                               // return code is in $?
}
